language: python
python:
  - "2.6"
  - "2.7"
install:
  - "pip install -r salt/roots/salt/jig/requirements.txt --use-mirrors"
  - "pip install -e ."
script:
  - "python script/test"
after_success:
  - "coveralls"
